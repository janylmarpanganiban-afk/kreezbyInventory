rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // For development, allow read/write access to all collections (including orders, inventory)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
